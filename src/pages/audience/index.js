import template from './template.html';

var controller = ['$scope', function($scope) {}];

var page = {
    path: '/audience',
    template: template,
    controller: controller
};

export default page;
